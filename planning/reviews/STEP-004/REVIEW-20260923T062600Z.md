---
schema: 1
kind: step_review
step_id: STEP-004
verdict: pass
reviewer_role: reviewer
created_at: 2026-09-23T06:26:00Z
reviewed_revision:
  git_head: f18fbf0dcb3438e89bde29a3a194d76ca122b646
  worktree_hash: sha256:ce1d3952ab0a34958b431bffddd2d925a31f4c0534f65ac6aeb4ff08dae6c7bb
specialized_reviews:
  gate_basis: sha256:05008078436542fbd3515d6a9c5187d5ff2a3baebc46657497d45ac6b03dc531
  required:
    - tests
  security: not_required
  security_evidence: null
  security_reason: "Deterministic preselector (`python3 .harness/tools/review_gates.py --json STEP-004`) на этой revision вернул `required=[tests]`, `reasons.security=[]`. Delta третьего прохода состоит из одной строки в `src/test/integration/extension.test.ts` (`update('sortOrder', 'id', ...)` → `update('sortOrder', undefined, ...)`) и удаления одного неиспользуемого ключа из `l10n/bundle.l10n.json`/`l10n/bundle.l10n.ru.json`. Ни trust boundary, ни network/shell/credential surface, ни containment (`src/projectModel/artifactIndex.ts`, `src/projectModel/manifestService.ts`) не затронуты — оснований для добровольного adversarial security pass нет."
  tests: pass
  tests_evidence: "Обязательный независимый `tests` reviewer был выполнен отдельным агентом на непосредственно предшествующей revision (`worktree_hash sha256:96d2b379...a301`, отчёт `planning/reviews/STEP-004/REVIEW-20260923T060500Z.md`) и вернул PASS без material findings. Для текущей revision (`sha256:ce1d3952...c7bb`) delta ограничена одной строкой integration-теста и двумя удалёнными l10n-ключами, поэтому tests-scope проверка выполнена мной поверх этого PASS и не принималась на слово: (1) независимо перезапущены `yarn test:unit` (exit 0, 88/88) и `yarn test:integration` (exit 0, EN и RU профили, по `8 passing` каждый); (2) проверено, что изменённая cancellation-часть сценария сохраняет дискриминирующую силу — `config().get('sortOrder')` при отсутствующем ключе возвращает контрибутируемый default `'id'`, а запись любого значения на cancel по-прежнему провалила бы assert; (3) инструментированным экспериментом в изолированной копии worktree (вне рабочего репозитория, с отключённым byte-restore и дампом байтов fixture перед `finally`) подтверждено, что residual `\"harnessNavigator.artifacts.sortOrder\": \"id\"` действительно исчез; (4) `git status --porcelain` до и после всех прогонов совпадает byte-for-byte. Coverage-границы (F-006e: `>1 valid root` в Extension Host; негативная ветка `isCopyableArtifactNode`) остаются теми же согласованными документированными gaps, что и в предыдущем PASS test review — delta их не расширяет."
  tests_reason: "step type=implementation; code/test surface changed"
---

# STEP REVIEW STEP-004 — 2026-09-23 06:26 UTC

## Scope checked

Третий проход. Предыдущие два immutable report (`REVIEW-20260923T053851Z.md`,
`REVIEW-20260923T060500Z.md`) уже независимо подтвердили F-001..F-006 первого
раунда устранёнными и целостность остальной реализации (Mutation policy,
Out of scope, ADR-004, REQ-004, `computeEmptyState` как single source of truth,
disposal hygiene). Полная перепроверка всей реализации с нуля не повторялась;
проверено следующее:

- Оба remaining findings второго отчёта на фактическом коде текущей revision:
  F-001 (evidence, byte-restore/`sortOrder` в committed fixture) и F-002
  (orphan l10n key `Select a Harness workspace root`).
- `src/test/integration/extension.test.ts` — sort/fallback/cancellation-блок
  целиком (строки ~432-475), весь остаток сценария до `finally` и сам
  `finally`-блок с комментарием о byte-restore.
- `l10n/bundle.l10n.json`, `l10n/bundle.l10n.ru.json` — полный worktree diff
  относительно `f18fbf0`, двусторонний анализ ключей (orphan-ключи EN bundle
  относительно всех литералов в `src`/`tests`, и обратно — все `l10n.t()`
  литералы и message-таблицы против EN bundle), RU ⊆ EN.
- Переписанный раздел Evidence в `planning/tasks/STEP-004.md` (основной блок
  `Команда: yarn test:integration` и новая секция
  `### Второй fix-review цикл`) против фактически наблюдённых результатов.
- Deterministic gates: `review_gates.py`, `validate.py --mode manual`,
  `sync-projections.py --check`, `git diff --check`,
  `planning-state.py review-revision`.
- Инструментированный эксперимент в изолированной копии worktree (вне рабочего
  репозитория): отключённый byte-restore + дамп байтов
  `src/test/fixtures/multi-root.code-workspace` непосредственно перед
  `finally`-restore, оба locale-профиля.

## Findings

Material findings отсутствуют.

Non-blocking observation (не material finding, verdict не меняет): последнее
предложение переписанного абзаца Evidence в `planning/tasks/STEP-004.md`
(«Byte-restore в `finally` сохранён как defensive safety net на случай других
незавершённых записей/падения теста в середине сценария, а не потому что он
необходим для sortOrder-пути конкретно») характеризует restore как страховку
«на случай», тогда как эксперимент показывает, что даже при полностью успешном
прогоне VS Code оставляет в committed fixture пустую обёртку `"settings": {}`,
то есть restore нужен каждому прогону, а не только аварийному. Утверждение
остаётся буквально верным в своей оговорке («для sortOrder-пути конкретно»), и
корректный факт зафиксирован трижды в других местах: в том же абзаце
(«byte-restore ... остаётся load-bearing, а не defensive-only»), в секции
`### Второй fix-review цикл` («restore фактически load-bearing, а не
defensive-only») и в комментарии над самим restore
(`src/test/integration/extension.test.ts`, `finally`), который называет точную
причину — необудаляемую обёртку `"settings": {}`. Поэтому документ в целом не
утверждает ложного факта и не вводит читателя в заблуждение; рекомендуемая
(необязательная) правка — убрать слово «defensive» из этого предложения или
дополнить перечисление причин пустой обёрткой `"settings": {}`.

## Verification observations

Проверка F-001 второго отчёта (evidence о byte-restore и residual `sortOrder`):

- Фактический fix присутствует: sortOrder-блок сценария завершается
  `await config().update('sortOrder', undefined, vscode.ConfigurationTarget.Workspace);`
  (`src/test/integration/extension.test.ts`, сразу после fallback-проверки
  `readSortOrder` и перед cancellation-блоком). Промежуточные записи
  `'status'` / `'id'` / `'not-a-real-sort-order'` остались — они внутри
  сценария и перекрываются финальным удалением ключа.
- Логика корректна и подтверждена прогоном: `config().get('sortOrder')` при
  отсутствующем ключе возвращает контрибутируемый `default: "id"` из
  `package.json`, поэтому следующая строка
  `const sortOrderBeforeCancel = config().get('sortOrder')` читает `'id'`, а
  cancellation-assert сохраняет прежнюю дискриминирующую силу (запись любого
  значения на cancel провалила бы assert так же, как и до правки).
- Инструментированная проверка (изолированная копия worktree, byte-restore
  отключён, байты fixture сдампены перед `finally`): committed fixture больше
  не содержит `"harnessNavigator.artifacts.sortOrder": "id"` — остаётся ровно
  пустая обёртка:
  ```
  {
    "folders": [
      { "path": "valid-project" },
      { "path": "ordinary-folder" },
      { "path": "invalid-release-project" },
      { "path": "non-regular-manifest-project" }
    ],
    "settings": {
    }
  }
  ```
  Все 8 тестов в обоих профилях при этом прошли (`8 passing` × 2). Тем самым
  дефект, зафиксированный F-001 второго отчёта (residual значение ключа),
  устранён по факту, а не по заявлению; одновременно экспериментально
  подтверждено, что комментарий над restore в `finally` («VS Code не удаляет
  обёртку `"settings": {}`») точен, и restore остаётся необходимым для
  byte-чистоты committed fixture.
- Переписанный абзац Evidence проверен по пунктам: заявление о причине
  (последний `update()` писал явное `'id'` вместо удаления ключа), о самом
  fix (`'id'` → `undefined`), о сохранении поведения cancellation-проверки и о
  чистом `git status -- src/test/fixtures` после прогона — все соответствуют
  фактам. Запрещённой реконструкции вывода (`AGENTS.md` §8) в новом тексте
  нет: приводятся `Команда`/`Exit code`/`Observed`, а спорное утверждение
  первого раунда («diff без restore теперь тоже чист») удалено.

Проверка F-002 второго отчёта (orphan l10n key):

- `grep -rn 'pickFolder\|Select a Harness workspace root' src tests l10n package.nls*.json package.json`
  не даёт ни одного совпадения (exit 1).
- Полный worktree diff обоих bundle относительно `f18fbf0` — по одному hunk в
  каждом файле (`@@ -19,5 +19,27 @@` и `@@ -18,5 +18,27 @@`), состоящему
  только из добавления новых ключей STEP-004 и запятой в предыдущей строке;
  ни один pre-existing ключ не изменён, не переименован и не удалён.
- Двусторонний анализ ключей: EN bundle — 43 ключа, RU — 42; orphan-ключей в
  EN относительно всех литералов в `src`/`tests` нет; все используемые
  литералы присутствуют в EN bundle. Единственный EN-only ключ —
  `Harness Navigator localization fallback is active.` — pre-existing (есть в
  `HEAD:l10n/bundle.l10n.json`, 21 ключ) и намеренно непереводимый fallback.
- `tests/unit/localization.test.ts` (RU ⊆ EN, непустые значения, required keys,
  запрещённые canonical-паттерны) проходит в общем прогоне.

Deterministic gates, перезапущенные мной на этой revision:

- `yarn typecheck` — exit 0, без вывода.
- `yarn lint` — exit 0, без вывода.
- `yarn format` — exit 0, `Checking formatting... All matched files use Prettier code style!`.
- `yarn test:unit` — exit 0, `tests 88`, `pass 88`, `fail 0`, `cancelled 0`,
  `skipped 0`, `todo 0`, `duration_ms 269.744922`.
- `yarn test:integration` — exit 0, два runner-прогона (EN и RU), в каждом
  `8 passing`, включая оба сценария Artifacts View/Focus View.
- `python3 .harness/tools/validate.py --mode manual` — exit 0,
  `HARNESS VALIDATION: PASS (259 tracked files checked, mode=manual)` с теми же
  pre-existing warnings `STEP-001`/`STEP-008 ready plan context_basis is stale`.
- `python3 .harness/tools/sync-projections.py --check` — exit 0, `PASS`.
- `git diff --check` — exit 0, без вывода.
- `python3 .harness/tools/review_gates.py --json STEP-004` — `required=["tests"]`,
  `reasons.security=[]`, `surfaceMode=worktree`,
  `basis=sha256:05008078...dc531`. Обязательный reviewer не снимался.
- `python3 .harness/tools/planning-state.py review-revision` —
  `git_head=f18fbf0...`, `worktree_hash=sha256:ce1d3952...c7bb`; отличается от
  `sha256:96d2b379...a301` второго прохода, то есть это действительно новая
  revision.
- `git status --porcelain` до и после всех прогонов и эксперимента совпадает
  byte-for-byte; рабочее дерево этим review не изменялось.

Ограничения доказательств:

- Эксперимент с отключённым restore выполнялся в изолированной копии worktree
  в scratchpad, а не в рабочем репозитории; рабочий `src/test/integration/extension.test.ts`
  не редактировался.
- Coverage-границы прежние и не расширены delta: поведение обоих tree provider
  при `>1 valid root` в работающем Extension Host не наблюдается (fixture
  содержит один valid Harness root) — согласованный документированный gap
  F-006e; негативная ветка `isCopyableArtifactNode` остаётся непокрытой
  (non-blocking observation предыдущего test review).
- Обязательный независимый `tests` reviewer выполнялся отдельным агентом на
  предшествующей revision; для delta текущей revision tests-scope проверка
  выполнена мной (см. `specialized_reviews.tests_evidence`).

## Verdict rationale

Оснований для `BLOCKED` нет: contract-конфликта, невыполнимого acceptance,
отсутствующего решения/prerequisite или stale planning context не обнаружено —
`plan.status=ready` с matching planning-review сохраняется, canonical gates
(`validate.py --mode manual`, `sync-projections.py --check`) зелёные,
обязательный `tests` reviewer присутствует и имеет статус `pass`.

Оба remaining findings второго прохода устранены по факту, а не по заявлению.
F-001: residual `"harnessNavigator.artifacts.sortOrder": "id"` в committed
fixture исчез — это подтверждено инструментированным прогоном с отключённым
restore, а не только чтением кода; заменяющий вызов
`update('sortOrder', undefined, ...)` не ослабляет последующую
cancellation-проверку, потому что `get()` при отсутствующем ключе возвращает
контрибутируемый default `'id'`; переписанный Evidence фиксирует точную
причину и не содержит реконструированного вывода. F-002: ключ
`Select a Harness workspace root` отсутствует в обоих bundle, и это
единственное изменение в них — pre-existing структура и остальные ключи не
затронуты, orphan-ключей в EN bundle не осталось, RU ⊆ EN сохраняется.

Регрессий delta не внесла: typecheck, lint, format, 88/88 unit, 8/8
integration в обоих locale-профилях, оба Harness-валидатора и
`git diff --check` зелёные на этой самой revision, рабочее дерево не
изменилось. Единственное замечание — неточная характеристика byte-restore как
«defensive safety net» в одном предложении Evidence — не является material:
корректный факт зафиксирован трижды в других местах того же документа и в
комментарии к коду, поэтому ложного утверждения документ не несёт и ни одно
решение на нём не строится. Material findings отсутствуют, поэтому verdict —
**pass**.
