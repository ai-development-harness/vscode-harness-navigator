---
schema: 1
kind: step_review
step_id: STEP-008
verdict: pass
reviewer_role: reviewer
reviewed_revision:
  git_head: 65d3401f5010c6a2a19dce7931a57ba890a5461c
  worktree_hash: sha256:8135977730fa2a79721ab7df2c8442883ca68f47d87232b94a5d154e8e156b4a
specialized_reviews:
  gate_basis: sha256:ddc9b08af424f1a1acc33d90af63a1c7c50b68e538c2137c6d9278e8299df36e
  required:
    - security
    - tests
  security: pass
  security_evidence: "Production manifestService.ts не менялся в FIX pass (подтверждено по mtime и построчно). Adversarial mutation testing в изолированной scratchpad-копии: 4 отдельные мутации (удаление deriveOpenedPath-enforcement; нейтрализация production wiring; удаление portable dev/ino check; удаление pre-open realpath+containment) — каждая роняет ровно тот новый/существующий тест, который должен её ловить, и не более того. Новые F-002 тесты бьют именно в manifest-path resolution detectProject, не в resolveContainedPath; leak-check через JSON.stringify(project) покрывает messageArguments/detail. Зеркального non-Linux ancestor-race assertion нет. ADR-005 Security implications правка узкая (только формулировка, Decision/Alternatives/Consequences не тронуты) и фактически точна (hard-link case проверен эмпирически на всех трёх профилях). Новых зависимостей/package.json изменений нет. Два low findings (F-S3, F-S4) не меняют verdict по явной оценке самого security review."
  security_reason: "risk_flags=security-sensitive"
  tests: pass
  tests_evidence: "yarn test:unit — 34/34 pass (было 29), воспроизведено независимо test review. Adversarial mutation testing: удаление deriveOpenedPath-ветки роняет ровно 1 из 34 тестов (новый ancestor-swap test, actual=invalidManifest вместо configurationBlocked) — подтверждает, что тест load-bearing, а не декоративный. Новые F-002 тесты делают manifest.yaml статическим symlink на внешний файл (до вызова detectProject, не через beforeOpen race hook) под Windows- и macOS-shaped capability; F-003 добавляет DARWIN_LIKE_CAPABILITY с реальными POSIX-флагами и deriveOpenedPath: undefined, используется в двух behavioral tests, покрывающих различные guard-ветки (ELOOP vs dev/ino). Все 7 Acceptance criteria STEP-008 перепроверены против текущих тестов — непокрытых не осталось. yarn lint и validate.py — чисто."
  tests_reason: "step type=bugfix; code/test surface changed"
created_at: 2026-09-21T20:42:02Z
---

# STEP REVIEW STEP-008 — 2026-09-21 20:42

## Scope checked

- Повторный проход после `STEP FIX STEP-008` (`fixReviewCycles: 1` из `maxFixReviewCycles: 3`),
  адресующего все 4 finding предыдущего `REVIEW-20260921T202606Z.md` (verdict `fail`).
- Три независимых read-only прохода: general correctness/architecture, mandatory `security`
  (adversarial mutation testing), mandatory `tests` (adversarial mutation testing) — каждый
  запустил реальные verification commands самостоятельно и не полагался только на
  `planning/tasks/STEP-008.md → Evidence`.
- `src/projectModel/manifestService.ts` (production, подтверждено неизменным с момента
  предыдущего review — byte-for-byte по mtime и по цитируемым конструкциям) и
  `tests/unit/manifestService.test.ts` (34 теста, +5 к предыдущему прогону) целиком.
- `docs/adr/ADR-005-platform-scoped-manifest-containment.md` — узкая правка формулировки
  (F-004 предыдущего review), Decision/Alternatives/Consequences не тронуты.
- Mutation policy compliance по file mtime relative к предыдущему review report
  (`created_at: 2026-09-21T20:26:06Z`): изменены только
  `tests/unit/manifestService.test.ts`, `docs/adr/ADR-005-*.md`,
  `planning/tasks/STEP-008.md` (Evidence). Production code, `package.json`, `yarn.lock`,
  integration fixtures и l10n bundles — не тронуты.
- Все 7 Acceptance criteria `STEP-008` сверены с текущим test suite построчно (independent
  test review); traceability REQ-002/REQ-008/ADR-001/ADR-005 ↔ STEP-008 подтверждена
  двусторонней; dependency `STEP-001` — `status: completed`.

## Findings

При PASS material semantic contradictions отсутствуют.

## Verification observations

- `python3 .harness/tools/planning-state.py review-revision` — `git_head
  65d3401f5010c6a2a19dce7931a57ba890a5461c`, `worktree_hash
  sha256:8135977730fa2a79721ab7df2c8442883ca68f47d87232b94a5d154e8e156b4a`; подтверждено до и
  после всех трёх review passes без изменений.
- `python3 .harness/tools/review_gates.py STEP-008 --json` — exit code `0`; `required:
  [security, tests]`, `gate_basis` идентичен предыдущему review.
- `yarn typecheck`, `yarn lint`, `yarn format`, `git diff --check` — exit code `0` во всех трёх
  independent passes.
- `yarn test:unit` — exit code `0`; `34 tests`, `34 pass`, `0 fail`. Воспроизведено трижды
  независимо с идентичным результатом.
- `python3 .harness/tools/validate.py --mode manual` — exit code `0`; `HARNESS VALIDATION: PASS
  (216 tracked files checked, mode=manual)`; warnings: stale `plan.context_basis` для
  `STEP-001`, `STEP-002` **и STEP-008** (новое). Причина прослежена general review: FIX-правка
  `ADR-005 → Security implications` входит в `planning_context_snapshot` (ADR-текст — часть
  contextBasis), тогда как `content_hash` (сам Implementation plan) не изменился и остаётся
  совпадающим со stamped `plan.content_hash`. Ready-plan proof chain (`stamp-plan`'s matching
  PASS planning-review) не инвалидирован: Decision/Scope этого plan не зависят от правки
  formulировки. Не blocking; рекомендуется `stamp-plan`-эквивалент re-basis отдельным
  action-item, не входящим в текущий review scope.
- Security review провёл 4 независимые mutation-мутации в изолированных scratchpad-копиях
  (не в рабочем дереве): удаление `deriveOpenedPath`-enforcement, нейтрализация production
  wiring, удаление portable dev/ino check, удаление pre-open `realpath`+`isContained` — каждая
  роняет ровно ожидаемый тест и не более. Test review независимо воспроизвёл mutation A
  (удаление `deriveOpenedPath`-ветки) с идентичным результатом (1/34 fail).
- General review отдельно подтвердил F-010 структурную гарантию: инъекция незарегистрированного
  message literal в scratchpad-копии ломает `tsc` (`error TS2345`), не только unit test.

### Низкоприоритетные наблюдения (не formal findings, не влияют на verdict)

- **F-S3** (security, low): формулировка `ADR-005 → Security implications`, первое
  предложение, всё ещё можно прочитать как «на Linux остаточного риска нет», хотя hard-link
  case (то же предложение, пункт 2) применим и к Linux тоже. Чисто документационная точность;
  рекомендация — уточнить формулировку в следующем прикосновении к этому файлу.
- **F-S4** (security, low): leak-check assertion в нескольких тестах
  (`assert.doesNotMatch(project.diagnostic.message, /banana/u)`) проверяет поле, в которое
  внешнее значение никогда не попадает (оно идёт в `messageArguments`); реальную регрессию
  ловит соседний `assert.equal(project.kind, ...)`, а не эта строка. Рекомендация — привести к
  паттерну `doesNotMatch(JSON.stringify(project), ...)`, уже используемому в новых F-002 тестах.
- (general review) Новый ancestor-swap test жёстко предполагает POSIX (`symlinkSync`) и не
  имеет platform-guard — на реальном non-Linux dev-раннере он бы упал, а не skip-нулся, в
  отличие от FIFO-теста. Не дефект (нет cross-platform product CI сегодня), но hardening-нит.
- (carried over из предыдущего review, не formal finding) `docs/architecture.md`'s Accepted
  ADR index (`:43-48`) по-прежнему не включает `ADR-005`; вне Mutation policy `STEP-008`,
  рекомендуется отдельный `PROJECT QUICK FIX`/docs sync.

## Verdict rationale

Verdict — `PASS`.

Все 4 finding предыдущего `REVIEW-20260921T202606Z.md` (`fail`) закрыты доказуемо, а не
номинально: F-001 и F-002 подтверждены independent mutation testing в двух отдельных review
passes (security и tests) с идентичным результатом — соответствующий guard действительно
load-bearing, удаление даёт ровно предсказанный regression (`invalidManifest`/`valid` с
внешним содержимым вместо `configurationBlocked`). F-003 добавляет поведенческий тест под
буквальным macOS-shaped profile, доказанно покрывающий отдельную guard-ветку (`ELOOP`,
не `dev`/`ino`). F-004 — узкая, фактически точная правка `ADR-005`, не меняющая Decision.

Production `manifestService.ts` не изменился в этом FIX pass (подтверждено тремя независимыми
проходами по mtime и по built-in построчной сверке) — весь risk security-sensitive кода уже
прошёл adversarial review в предыдущем цикле и остался нетронут. Mutation policy соблюдён
буквально. Все Acceptance criteria `STEP-008` теперь имеют доказанное (не просто заявленное)
test-покрытие, включая ранее недоказанную Linux-specific ancestor-race защиту.

Два новых low-severity finding от security review (F-S3, F-S4) и низкоприоритетные
наблюдения general review — документационная точность и test-assertion precision, явно
оценённые самим security review как не влияющие на его собственный `pass`; ни один не
затрагивает Acceptance, ADR Decision или production-поведение. Оставлены как задокументированные
follow-up для будущего прохода, а не formal findings (что и не позволило бы схема `PASS`).

`STEP-008` готов к completion proof (independent PASS review + type-specific evidence).
Следующий шаг по `STEP RUN STEP-008`: closing/finalization (canonical `status: completed`
после тех же deterministic gates), а не новый FIX/REVIEW цикл.
