---
schema: 1
kind: release_check
target: 0.0.1
verdict: ready
created_at: 2026-09-24T17:47:02Z
---

# Release Check — 2026-09-24

Цель: `vscode-harness-navigator` `0.0.1` (VSIX), коммит `d5add9f` (`main`). Release model: ручной чеклист из `docs/development.md`, публикация (`vsce publish`, tag, Marketplace/Open VSX) вне scope и делается владельцем вручную.

## Requirements / scope

- Все 15 STEP (`STEP-001`…`STEP-015`) имеют `status: completed`; у каждого последний review-отчёт в `planning/reviews/STEP-NNN/` имеет `verdict: pass`.
- Все REQ (`REQ-001`…`REQ-010`) в `docs/requirements/STATUS.md` имеют статус `completed`.
- Открытых OQ нет (`docs/open-questions/` содержит только шаблон).
- Незавершённых release-critical STEP нет.

## Verification gates

Все автоматические gates из release checklist выполнены на чистом рабочем дереве (`git status` пуст):

- `yarn install --immutable` — PASS
- `yarn typecheck` — PASS
- `yarn lint` — PASS
- `yarn format` — PASS
- `yarn test:unit` — PASS (166 pass, 0 fail)
- `yarn build` и `yarn package` — PASS
- `yarn inspect:package` — PASS (0 violations, `extension/readme.md` совпадает с `docs/marketplace/README.md`)
- `yarn test:integration` — PASS (23 passing в каждом из профилей integration-en/ru, mvp-en/ru)
- `yarn test:packaged` — PASS (24 passing в профилях packaged-en/ru)
- `git diff --check` — PASS
- `python3 .harness/tools/validate.py --mode manual` — PASS (454 файла); предупреждения `context_basis is stale` у ready plan выполненных STEP не влияют на release.
- CI на `main` для `d5add9f`: `CI` и `Harness Integrity` — success.

## Security / migrations / compatibility

- Расширение остаётся read-only и offline (ADR-001, ADR-004); containment по ADR-005/006/007. Новых зависимостей и изменений кода расширения после последнего STEP-review нет; последний коммит `d5add9f` обновляет только Harness-owned файлы (v0.8.3).
- Отдельный security review и аудит зависимостей в рамках этого прогона не выполнялись; такого gate в проекте нет.
- Миграций данных нет; `engines`: `vscode ^1.85.0`, `node >=22`.

## Unresolved blockers

Blocker'ов нет. Не проверено в этом прогоне (ручные пункты чеклиста, `docs/development.md`, п. 8–9), выполняет владелец перед публикацией:

- ручная проверка Status Bar / Summary в Extension Development Host (RU/EN, светлая/тёмная тема);
- ручная установка VSIX в чистый профиль: иконка в списке Extensions и README на вкладке Details.

## Evidence

- Команды и exit codes: каждая команда из «Verification gates» завершилась с exit code 0; полный вывод сохранён во временных логах сессии, не в репозитории.
- Первый прогон `validate.py` внутри моего скрипта завершился rc=1 из-за ошибки имени лог-файла (символ `/` в пути), а не из-за валидатора; повторный запуск отдельной командой дал PASS.
- `gh run list --branch main`: `CI` success и `Harness Integrity` success для `d5add9f`.
- Отчёт был восстановлен из сохранённого текста после того, как исходный файл пропал с диска при обновлении Harness до v0.8.4 (причина не установлена).
